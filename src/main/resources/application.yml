spring:
  application:
   name: code-genie
server:
  port: 8080
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.chen.codegenie.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
